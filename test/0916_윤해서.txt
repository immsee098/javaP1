[ java 9강 -1차 테스트, 종합 테스트]

1.  다형성이란
부모 클래스로 동일한 클래스의 부분을 관리하고 공통적으로 사용할 수 있는 것


2.  기본형 변수와 같이 참조형 변수도  형변환이 가능하다.
단, 서로 ( 상속관계  ) 사이에서만 가능하다.


3. 자식 타입의 참조변수를 부모 타입의 참조변수로, 부모 타입의 참조변수를 자식 타입의 참조변수로의 형변환만 가능하다.
자동형변환은 언제 발생하고, 명시적 형변환해야 하는 경우는 언제인가?
자식-> 부모로 자꿀 시 자동형변환
부모-> 자식은 꼭 명시적으로 형변환을 해줘야함


4. instanceof 연산자의 형식을 쓰시오. 
변수 instanceof 클래스명


5. instanceof 연산자에 대해 설명하시오.
참조하는 인스턴스가 뭐인지 알아볼 수 있게 도와주는 연산자다


6.  instanceof를 이용한 연산결과로 true를 얻었다는 것은 
(    해당 클래스 타입으로 형변환이 하능하다         )는 것을 뜻한다.


7. 다음 클래스를 이용하여 코드를 완성하시오.

class Shape {
	public double findArea() {
		return 0.0;
	}
}//

class Circle1 extends Shape {
	final double PI = 3.14;
	private int r;

	public Circle1(int r){
		this.r = r;
	}

	public double findArea() {
		return  PI*r*r;
	}

	public void showCircle(){
		System.out.println("Circle1의 메서드!");
	}
}//

class Rect1 extends Shape {
	private int w;
	private int h;

	public Rect1(int w, int h){
		this.w =w;
		this.h = h;
	}
	public double findArea() {
		return w*h;
	}
	public void showRect(){
		System.out.println("Rect1의 메서드!");
	}
}//

class JavaExam1 {
	public static void main(String[] args) {
		//1) Shape배열을 만들고, 요소 4개를 저장한 후, 각 요소의 오버라이딩 메서드 호출
		Shape[] sh = new Shape[4];
		sh[0] = new Circle1(10);
		sh[1] = new Rect1(10, 20);
		sh[2] = new Circle1(20);
		sh[3] = new Rect1(20,40);
		
		for(Shape n : sh) {
			System.out.println(n.findArea());
		}



		//2) 각 요소의 자식클래스만의 메서드 호출
		for(Shape n : sh) {
			if(n instanceof Rect1) {
				Rect1 re = (Rect1)n;
				re.showRect();
			} else if(n instanceof Circle1) {
				Circle1 ci = (Circle1)n;
				ci.showCircle();
			}
		}

	}
}



8. instanceof 연산자는 실제 인스턴스를 알아보기 위해 사용하는데, 
어떤 경우에도 결과가 true가 나오나?
아니다

==============================
1. 일반적인 프로그램은 Windows 나  Linux 같은 운영체제(Operating System) 위에서 실행 되므로
윈도우즈에서 동작하도록 구현된 워드 프로그램은 리눅스 기반에서 동작하지 않는다.
리눅스에서 동작시키려면 리눅스를 기반으로 다시 구현해야 한다.

이와 비교하여 자바 프로그램의 실행구조를 설명하시오.
자바는 자바 가상머신 위에서 동작하기 때문에 다른 운영체제로 갈아타더라도 코드의 수정 없이 재사용이 가능하다.


2.  연산자의 우선순위를 완성하시오
(  산술   ) > (  비교   ) > (   논리   ) > (   대입   )

(   단항    ) > (   이항    ) > (    삼항    ) 


3. 조건식으로 표현하시오
1) 문자열 참조변수 str이 "yes"일 때 true인 조건식
str.equals("yes")


2) char형 변수 ch가 숫자(‘0’~‘9’)일 때 true인 조건식
a>='0' && a<='9'

3) x가 0보다 크거나 x가 10보다 작다 
x>0 || x<10

4) boolean형 변수 powerOn이 false일 때 true인 조건식
if(!powerOn) {}


4. 기본자료형에서 등가연산자(==)는 값을 비교하지만
 참조형에서 등가연산자는 ( 주소값 )을 비교한다.
 문자열의 값을 비교하기 위해서는 String클래스의 ( equals ) 메소드를 이용한다.


5.  (  break    ) 는 반복문이나 switch문의 case를 벗어날 때 사용하며
무한 루프를 탈출할 때 종종 사용한다.
(      continue    )는 루프의 나머지 뒷 부분을 무시하고 반복문의 선두로 점프하여 다음 루프를 실행한다.
다음 반복위치로 이동하는 분기문으로 루프에서 특정 값에 대한 처리를 제외하고자 할 때 사용한다.


6. 메서드란
일정 기능을 하는 기능의 묶음 단위


7. (  return  )은 메서드의 실행을 종료하고 호출원으로 복귀하는 기능과
메서드의 처리 결과를 호출원으로 돌려주는 기능도 한다.


8. 오버로딩과 오버라이딩에 대해 설명하시오.
 오버로딩은 한클래스 내 같은 이름 메서드를 매개변수나 자료형 수를 다르게만 하여 여러개 사용하는 건
오버라이딩은 자식클래스가 부모클래스 메서드의 내용을 덮어쓰는 것

9. 배열이란
데이터를 한 번에 여러개 저장 가능한 저장소 같은 것

10. 자바의 다차원 배열에서 각 영역은 모두 reference 를 표시하는 공간이고, 
마지막 배열 공간만이 실제 데이터를 나타내는 공간이다.
2차원 배열 arr에서 행의 개수는 (    arr.length         ) 으로 구하고, 
열의 개수는 (    arr[0].length  ) 으로 구한다.


11. 클래스란?
객체를 형성하기 위한 틀 같은 것


12. ( )안에 공통으로 들어가는 용어는?
객체 지향 언어는 소스코드의 재사용이 쉽고, 실세계와도 유사한 개념인 객체로 프로그램을 구성한 것으로
필요한 기능을 부품처럼 미리 만들어 놓고, 필요한 경우 바로 사용하여 구현할 수 있다.
(   )는 클래스가 메모리에 load된 것으로 클래스는 코드, 이 코드를 메모리에 띄운 것이 (    )이다.
클래스는 직접 사용할 수 없고 클래스를 통해 (    ) 를 생성해야만 그 (    ) 를 사용할 수 있다.
~> 객체


13. ()안에 공통으로 들어가는 용어는?
어떤 클래스로부터 만들어진 객체를 그 클래스의 (               )라고 한다.
객체는 모든 (                 )를 대표하는 포괄적인 의미이고,
(             )는 어떤 클래스로부터 만들어진 것인지를 강조하는 보다 구체적인 의미이다.
~> 인스턴스


14. 생성자란?
멤버변수의 초기화를 해준데


15. 접근 제어자가 사용될 수 있는 곳은?
클래스,맴버변수, 메서드, 생성자


16. 4가지 접근 제한자를 접근 범위가 넓은 쪽에서 좁은 쪽의 순으로 왼쪽부터 나열하고,
각각에 대해 설명하시오.
public / protected / default / private


17. 지역변수에도 접근제한자를 사용할 수 있나?
불가능



18. static변수(클래스 변수)란?
인스턴스별로 다르게 가지는 게 아닌 클래스가 공통으로 공유하는 변수

19.  static 변수가 초기화되는 시점은?
자바 가상머신에 의해 코드가 올라갈 때


20. static 변수(클래스 변수)의 복잡한 초기화에 사용되는 것은 무엇이고, 언제 수행되나?
static 초기화 블럭 -> 자바가상머신에 의해 돌아가기 시작하는 순간임


21.  변수의 3가지 종류에 대해 각각 설명하시오. 해당 변수가 언제 생성되고, 소멸되는지도 설명하시오
z클래스 변수 - 계속 살아있음
인스턴스 변수 - 클래스가 올라갔을 시/클래스가 내려가면 소묠
지역 변수 - 메서드 시작 시/메서드 끝날 시


22. 상속이란?
부모 클래스의 것들을 자신의 것처럼 사용 가능

23. 상속에서 생성자가 실행되는 순서는?
가장 부모 상위에 있는 클래스의 생성자부터 실행


24. 기본형 매개변수와 참조형 매개변수를 비교하여 설명하시오.
기본형은 값이 복사
참조형은 주소가 복사된다. 따라서 한 곳에서 바꾸면 한까번에 바뀐다



25. 자바 가상머신의 메모리 모델에 대해 설명하시오
메모리를 데이터 특성에 따라 다르게 저장해준다


26. this와 super, this()/super() 를 비교하여 설명하시오.
this 자기 자신 호출 super은 부모 호출
this() 동일한 클래스 생성자 호출, super()부모 클래스 생성자 호출

27. final 이 사용될 수 있는 곳은?
클래스, 메서드, 변수들

28. 맞으면 O, 틀리면 X 표시하시오
final 을 지역변수로 선언하고자 한다면 static 도 함께 사용할 수 있다. ( O )
지역변수 앞에 final을 붙일 수 있다. (O )
지역변수 앞에 접근 제한자를 붙일 수 있다(X )


29. 클래스를 재사용하는 2가지 방법에 대해 설명하시오.
상속 이용(is a) / 포함 관계 이용(has a)

